<% @folders.each do |folder| %>
  <%= link_to folder.name, admin_user_folder_path(@admin_user, folder) %><br>
<% end %>

<%= link_to "フォルダ作成", new_admin_user_folder_path(current_admin_user), class:"btn btn-light" %><br>
      